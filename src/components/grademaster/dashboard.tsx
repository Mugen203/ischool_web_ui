"use client";

import type { User } from "@/types/auth";

export function GrademasterDashboard({ user }: { user: User }) {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Grademaster Dashboard</h1>
      <p>Grademaster dashboard coming soon...</p>
    </div>
  );
}
